<li>
	{{name}}

	<button class="change-devour d-inline btn btn-success" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!!{{else}}<button class="remove-food d-inline btn btn-success" data-id="{{id}}">
		DELETE
	</button>{{/if}}
	</button>
</li>
